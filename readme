该压缩包包含实验源程序与测试程序。

Parser包里面即为本次语法分析实验的源程序

助教可以直接运行其中test(与src包同一级)包中的Parser_Test来获得语法分析树
Collection_of_setsTest来获得所有的项目集
LRTableGen_Test来获得LRTable(ACTION,GOTO)

测试用例为test.txt
文法定义在grammar.txt

注意到:本次实验需要使用到测试框架，需要在lib中添加一些.jar，具体可看lib包