program'->program
program->block
block->{ decls stmts }
decls->decl decls
decls->epsilon
decl->type id ;
type->basic type'
type'->[ num ] type'
type'->epsilon
stmts->stmt stmts
stmts->epsilon
stmt->loc = bool ;
stmt->if ( bool ) stmt
stmt->while ( bool ) stmt
stmt->do stmt while ( bool ) ;
stmt->break ;
stmt->block